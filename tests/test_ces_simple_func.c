/**
 * Test simple de función sin recursión - Versión en Español
 */

entero suma(entero a, entero b) {
    entero resultado;
    resultado = a;
    resultado = b;
    return resultado;
}

entero main() {
    entero x;
    entero y;
    entero z;
    
    x = 5;
    y = 3;
    z = suma(x, y);
    
    retornar 0;
}
